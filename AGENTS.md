# Project Guidelines

## Git Workflow

After making code changes:
1. Run syntax check (`python3 -m py_compile <file>`)
2. `git add` changed files
3. `git commit -m "<description>"`
4. `git push`
5. Clean token from remote URL: `git remote set-url origin https://github.com/ar1gel/re_hub.git`
